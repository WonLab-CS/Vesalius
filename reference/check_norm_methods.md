# check if norm method is present in availble options

check if norm method is present in availble options

## Usage

``` r
check_norm_methods(norm, use_counts = NULL)
```

## Arguments

- norm:

  string - norm method parsed to function

- use_counts:

  string - which count matrix to use

## Value

norm method string or error
