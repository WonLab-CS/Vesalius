# create function log to be commit to the log slot

create function log to be commit to the log slot

## Usage

``` r
create_commit_log(arg_match, default)
```

## Arguments

- arg_match:

  function argument call

- default:

  default argument values of function

## Value

list with all arguments used in latest function call
