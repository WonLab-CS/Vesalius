# check number of spatial indices present

check number of spatial indices present

## Usage

``` r
check_min_spatial_index(group, min_spatial_index, id)
```

## Arguments

- group:

  count matrix

- min_spatial_index:

  numeric - min number of spatial indices that need to be present in
  group

- id:

  string - id of group
