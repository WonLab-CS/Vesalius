# merging batch matches together

merging batch matches together

## Usage

``` r
concat_matches(matched_indices, coms)
```

## Arguments

- matched_indices:

  list containing matched points in each batch

- coms:

  character - potential changes to be applied to data Attached to list
  as comment attribute

## Value

data.frame with matched points
