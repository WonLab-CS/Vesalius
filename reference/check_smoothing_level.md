# check if across level options are valid for smoothing

check if across level options are valid for smoothing

## Usage

``` r
check_smoothing_level(method)
```

## Arguments

- method:

  string - across level option

## Value

acroos level option or error
