# Internal function returning cell type label match as numiric

Internal function returning cell type label match as numiric

## Usage

``` r
match_cells(idx, query_labels, seed_labels)
```

## Arguments

- idx:

  numeric - used to iterate down the labels

- query_labels:

  cell type labels for query

- seed_labels:

  cell type labels for seed

## Value

binary numeric vector
