# Infer normalized counts from shared embedding values

Infer normalized counts from shared embedding values

## Usage

``` r
back_infer(counts, embeds)
```

## Arguments

- counts:

  list of count matrices containing normalized gene expression

- embeds:

  shared embedding matrix produced by Seurat.

## Value

list containing inferred count values from embeddings
