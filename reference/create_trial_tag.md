# create a trail tag name

create a trail tag name

## Usage

``` r
create_trial_tag(trials, tag)
```

## Arguments

- trials:

  character vector containing names of trials that have already been
  computed

- tag:

  character string describing which trial tag to add
