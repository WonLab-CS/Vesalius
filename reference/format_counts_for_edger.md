# format counts for edgeR

format counts for edgeR

## Usage

``` r
format_counts_for_edger(seed, query)
```

## Arguments

- seed:

  seed count matrix

- query:

  query count matrix

## Value

DGEList object from edgeR
