# importFrom imager nPix bubble_stack

importFrom imager nPix bubble_stack

## Usage

``` r
bubble_stack(coordinates, n_centers = 500, max_iter = 500)
```

## Arguments

- coordinates:

  data.frame containing spatial coordinates

- n_centers:

  numeric - number of centers to use for clustering

- max_iter:

  numeric - maximum number of iterations

## Value

clustered coordinates
