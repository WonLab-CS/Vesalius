# check if smoothing method is present in availble options

check if smoothing method is present in availble options

## Usage

``` r
check_smoothing_kernel(method)
```

## Arguments

- method:

  smoothing method selected by user

## Value

smoothing method string or error
