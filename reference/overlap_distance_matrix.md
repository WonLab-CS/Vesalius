# create a distance matrix based on mapping scores overlaps

create a distance matrix based on mapping scores overlaps

## Usage

``` r
overlap_distance_matrix(score, top_nn = 10, verbose = TRUE)
```

## Arguments

- score:

  matrix - cost matrix

- top_nn:

  integer nearest neighbors

- verbose:

  logical - print progress messages
