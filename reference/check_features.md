# check features

check features

check features

## Usage

``` r
check_features(counts)

check_features(counts)
```

## Arguments

- counts:

  a seurat object

## Value

a list of features

a list of features

## Details

If the user use custom counts, we want to check if variable features
have been computed or not. Dim reduction requires a list if features toi
be provided or variable features to be computed.

If the user use custom counts, we want to check if variable features
have been computed or not. Dim reduction requires a list if features toi
be provided or variable features to be computed.
