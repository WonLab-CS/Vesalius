# filter mapped cells

filter mapped cells

## Usage

``` r
filter_maps(mapped, allow_duplicates, verbose)
```

## Arguments

- mapped:

  data frame containing mapped cells

- allow_duplicates:

  logical - if duplicated matches are to be reatained

- verbose:

  logical - output progress messages

## Value

filtered mappped data frame
