# check if embedding method is present in availble options

check if embedding method is present in availble options

## Usage

``` r
check_embed_methods(embed)
```

## Arguments

- embed:

  embedding method selected by user

## Value

embedding method string or error
